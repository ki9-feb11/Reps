<h2>投稿詳細</h2>

<p><strong>投稿者：</strong> <%= @post.user&.email || "未登録ユーザー" %></p>
<p><strong>タイトル：</strong> <%= @post.title %></p>
<p><strong>内容：</strong><br><%= simple_format(@post.body) %></p>
<p><strong>投稿日：</strong> <%= @post.created_at.strftime("%Y/%m/%d %H:%M") %></p>

<%= link_to "戻る", admin_posts_path, class: "btn btn-secondary mt-3" %>
<%= link_to "削除", admin_post_path(@post), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger mt-3" %>
