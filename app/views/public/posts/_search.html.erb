<%= form_with url: posts_path, method: :get, local: true do |f| %>
  <%= f.text_field :keyword, placeholder: "キーワード検索（投稿・商品名）" %>
  <%= f.collection_select :tag_id, Tag.all, :id, :name, include_blank: "タグを選択" %>
  <%= f.submit "検索" %>
<% end %>