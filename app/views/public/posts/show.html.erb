<h2><%= @post.title %></h2>

<p><strong>内容：</strong><%= @post.content %></p>
<p><strong>訪問日：</strong><%= @post.visited_on.strftime("%Y/%m/%d") %></p>
<p><strong>顧客の反応：</strong>
  <%= @post.reaction_rating %> / 5
  <% @post.reaction_rating.to_i.times { %><i class="fas fa-star text-warning"></i><% } %>
  <% (5 - @post.reaction_rating.to_i).times { %><i class="far fa-star text-warning"></i><% } %>
</p>

<p><strong>タグ：</strong>
  <% @post.tags.each do |tag| %>
    <span class="badge bg-secondary"><%= tag.name %></span>
  <% end %>
</p>

<p><strong>商品・数量：</strong></p>
<ul>
  <% @post.post_products.each do |pp| %>
    <li><%= pp.product.name %>（<%= pp.quantity %>個）</li>
  <% end %>
</ul>

<div class="mt-4 d-flex gap-3">
  <%= link_to "編集", edit_post_path(@post), class: "btn btn-outline-secondary" %>
  <%= link_to "削除", post_path(@post), method: :delete, data: { confirm: "削除してもよろしいですか？" }, class: "btn btn-danger" %>
  <%= link_to "一覧に戻る", posts_path, class: "btn btn-secondary" %>
</div>